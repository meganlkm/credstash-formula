# Default lookup dictionary
aws:
  region: us-east-2
  account_id: ''
credstash:
  table_name: credential-store
  iam_setup:
    policy_name: credstash-setup-policy
    role_name: credstash-setup-role
